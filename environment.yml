name: cca
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - pyyaml
  - meshcat-python
  - pinocchio
  # Build tooling for the optional C++ reference binding used by tests/
  - pybind11
  - cmake
  - ninja
